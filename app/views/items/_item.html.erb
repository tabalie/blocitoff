<%= content_tag :div, class: 'media', id: "item-#{item.id}" do %>

  <div class= 'media-body'>
    <h5 class= 'text-center'><%= item.name %></h5>
    <h6><%= link_to "Mark task as completed.", [item.user, item], method: :delete, class: 'glyphicon glyphicon-ok', remote: true %></h6>
  </div>
<% end %>
